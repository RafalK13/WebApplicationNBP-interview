import "./Header.scss";

const headerDescription = "Average exchange rates - recruitment task";

const Header = () => {
  return (
    <header className="container">
      <h1>{headerDescription}</h1>
    </header>
  );
};

export default Header;
